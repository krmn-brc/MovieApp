 @model Movie;

    <div class="card mb-3" style="max-width: 540px;">
            <div class="row g-0">
                <div class="col-md-4">
                <img src="/images/@Model.ImageUrl" class="img-fluid rounded-start" alt="@Model.Title">
                </div>
                <div class="col-md-8">
                <div class="card-body">
                    <h5 class="card-title">@Model.Title</h5>
                    <div class="card-text">
                        <a asp-controller="Movies" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-primary">Detay</a>
                        <a asp-controller="Movies" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Güncelle</a>
                        <form asp-controller="Movies" asp-action="Delete" method="post" style="display:inline;">
                            <input type="hidden" asp-for="Id" value="@Model.Id">
                            <input type="hidden" asp-for="Title" value="@Model.Title">
                            <button type="submit" class="btn btn-danger">Filmi Sil</button>
                        </form>
                    </div>
                   
                   
                </div>
                </div>
            </div>
    </div>
        